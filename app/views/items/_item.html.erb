<% cache(item) do -%>
<tr>
  <td><% unless item.first? %>&nbsp;
  <%= link_to image_tag('up.png', alt: 'Up'), {action: 'up', id: item}, class: 'pure-button' %>
  <% end %></td>
  <td><% unless item.last? %>&nbsp;
  <%= link_to image_tag('down.png', alt: 'Down'), {action: 'down', id: item}, class: 'pure-button' %>
  <% end %></td>
  <td><%= link_to item.name, edit_item_path(item) %></td>
  <td>£<%= item.price_in_pounds %></td>
  <td class="actions"><%= delete_button(item) %></td>
</tr>
<% end -%>
