<% cache(item) do -%>
<tr>
  <td><% unless item.first? %><%= up_button(item) %><% end %></td>
  <td><% unless item.last? %><%= down_button(item) %><% end %></td>
  <td><%= link_to item.name, edit_item_path(item) %></td>
  <td>£<%= item.price_in_pounds %></td>
  <td class="actions"><%= delete_button(item) %></td>
</tr>
<% end -%>
